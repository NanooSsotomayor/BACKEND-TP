# Use Git LFS for large binary files if you decide to enable LFS
# Example patterns (uncomment and run `git lfs install` and `git lfs track "*.pbf"` if using LFS)
# *.pbf filter=lfs diff=lfs merge=lfs -text
# *.osrm filter=lfs diff=lfs merge=lfs -text
# *.db filter=lfs diff=lfs merge=lfs -text

# Ensure consistent end-of-line for scripts
*.sh text eol=lf
*.ps1 text eol=crlf

